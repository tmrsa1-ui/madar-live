# Optional: OpenSky OAuth2 API client (Account page -> API clients on opensky-network.org).
# Without these the proxy uses anonymous access (400 credits/day, 10 s resolution).
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
